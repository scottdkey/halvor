# Radarr 4K Instance Values
# Deploy with: halvor helm install radarr --name radarr-4k -f releases/radarr-4k.yaml

service:
  port: 7879

persistence:
  media:
    path: /mnt/media/movies-4k
  downloads:
    path: /mnt/downloads

env:
  PUID: "1000"
  PGID: "1000"
  TZ: "America/Chicago"
